---
title: "Untitled"
format: html
editor: visual
---

## Quarto

Quarto enables you t content and executable code into a finished document.
To learn more about Quarto see <https://quarto.org>.

Quarto enables you to weave together content 


## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code.
You can embed code like this:

```{r}
1 + 1
```



The `echo: false` option disabbles the prrinting of code (onlly output is displayed).


You can like this options to executable code like this
